<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:context=".MainActivity"
        android:orientation="vertical"
        android:layout_marginStart="30dp"
        android:layout_marginEnd="30dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="30dp"
            android:fontFamily="sans-serif"
            android:gravity="center"
            android:text="Setup Font Keyboard"
            android:textSize="@dimen/main_activity_text_ize"
            android:textStyle="bold" />

        <Button
            android:layout_width="match_parent"
            android:layout_gravity="center"
            android:layout_height="wrap_content"
            android:background="@color/colorSelected"
            android:textColor="@color/colorWhite"
            android:id="@+id/setupKeyboard"
            android:textSize="@dimen/main_activity_view_ize"
            android:text="ENABLE FONTS KEYBOARD"/>

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorSelected"
            android:textColor="@color/colorWhite"
            android:layout_marginTop="@dimen/margin10"
            android:textSize="@dimen/main_activity_view_ize"
            android:layout_gravity="center"
            android:id="@+id/selectKeyboard"
            android:text="SELECT FONTS KEYBOARD"/>

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Welcome to Font Keyboard"
            android:id="@+id/tvKeyboard"
            android:textSize="@dimen/main_activity_view_ize"
            android:layout_marginTop="30dp"
            android:inputType="textMultiLine"
            />

    </LinearLayout>
</ScrollView>